/**
 * @description This is the faculty object.
 * All the Info that is being returned.
 */

export type Faculty = {
    name: string;
    title: string;
    departments: string[];
    imageUrl: string;
}